[package]
name = "router-common"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
crate-type = ["rlib"]

[dependencies]
soroban-sdk = { workspace = true }
ed25519-dalek = ">=2.0.0, <3"

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
